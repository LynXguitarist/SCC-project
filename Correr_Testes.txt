COMO CORRER TESTES EM AZURE:

	az login
	mvn build
	mvn deploy
	usar Test<ClassName> para testar do CosmosDB

Para testar a API, usar testes artillery:
	Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
	artillery run test-images.yml

Para testar com o Postman corra o server(scc.server.server) e o resto já sabe,